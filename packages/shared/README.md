# Shared

Esta pasta vai concentrar contratos compartilhados entre os servicos.

Como a API usa TypeScript e a Function usa Python, o compartilhamento principal sera por contrato e documentacao, por exemplo:

- schemas JSON
- exemplos de payload WebSocket
- convencoes de eventos
