apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: web-socket
spec:
  selector:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: web-socket
  ports:
    - name: http
      port: 3000
      targetPort: http
